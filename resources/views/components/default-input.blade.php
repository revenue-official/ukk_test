<div class="">
    <label for="{{ $forId }}" class="form-label"></label>
    <input class="form-control" type="{{ $type }}" name="{{ $forId }}" id="{{ $forId }}"
        placeholder="{{ $placeholder }}" autocomplete="{{ $autocomplete }}" value="{{ $value }}">
</div>
